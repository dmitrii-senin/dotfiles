---
description: Perf learning (x86 performance) — runs the learn engine for the perf domain
argument-hint: "mm [area] | flash [area] | drill [area] | cheatsheet [name] | status"
---
Run the **learn** skill for the `perf` domain.

Treat this exactly as `/learn perf $ARGUMENTS`: read `~/.claude/skills/learn/SKILL.md`
and follow its dispatcher with domain = `perf` and the remaining arguments
`$ARGUMENTS` (mode / area / topic), then execute the requested mode.
