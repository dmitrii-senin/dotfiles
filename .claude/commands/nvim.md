---
description: Neovim 0.12+ learning — runs the learn engine for the neovim domain
argument-hint: "mm <area> | drill <area> | cheatsheet <name> | audit [focus] | status  (areas: core navigation lsp config languages tooling)"
---
Run the **learn** skill for the `neovim` domain.

Treat this exactly as `/learn neovim $ARGUMENTS`: read `~/.claude/skills/learn/SKILL.md`
and follow its dispatcher with domain = `neovim` and the remaining arguments
`$ARGUMENTS` (mode / area / topic), then execute the requested mode.
