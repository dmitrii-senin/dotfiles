---
description: Claude Code & LLM learning — runs the learn engine for the claude domain
argument-hint: "mm <area> | challenge | audit [focus] | update <target> | status  (areas: prompt skill workflow llm)"
---
Run the **learn** skill for the `claude` domain.

Treat this exactly as `/learn claude $ARGUMENTS`: read `~/.claude/skills/learn/SKILL.md`
and follow its dispatcher with domain = `claude` and the remaining arguments
`$ARGUMENTS` (mode / area / topic), then execute the requested mode.
