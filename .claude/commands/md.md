---
description: CME market-data infrastructure learning — runs the learn engine for the market-data domain
argument-hint: "mm <area> | flash <area> | drill <area> | challenge | cheatsheet <name> | status  (areas: cme-mdp sbe feed-handler lowlat-net orderbook systems measurement ownership)"
---
Run the **learn** skill for the `market-data` domain.

Treat this exactly as `/learn market-data $ARGUMENTS`: read `~/.claude/skills/learn/SKILL.md`
and follow its dispatcher with domain = `market-data` and the remaining arguments
`$ARGUMENTS` (mode / area / topic), then execute the requested mode.
